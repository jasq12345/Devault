spring.application.name=workspace

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/workspace_db
spring.datasource.username=${DB_USERNAME_AUTH:workspace_user}
spring.datasource.password=${DB_PASSWORD_AUTH:workspace_password}

spring.jpa.hibernate.ddl-auto=update
