spring.application.name=workspace

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:workspace_db}
spring.datasource.username=${DB_USER:workspace_user}
spring.datasource.password=${DB_PASSWORD:workspace_password}

spring.jpa.hibernate.ddl-auto=update
